name: wf3

on:
  push:
    branches:
      - tst

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check Out Repository
      uses: actions/checkout@v2

    - name: Run a Command
      run: echo "Hello, GitHub Actions!"
